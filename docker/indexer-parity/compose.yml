# Code generated by Python script. DO NOT EDIT.
services:
  baseline:
    command:
    - indexer
    depends_on:
      db-baseline:
        condition: service_healthy
    environment:
      NPRISM_CARDANO_NETWORK: ${NEOPRISM_PARITY_NETWORK:-preprod}
      NPRISM_CARDANO_RELAY_ADDR: ${NEOPRISM_PARITY_RELAY_ADDR:-preprod-node.play.dev.cardano.org:3001}
      NPRISM_CONFIRMATION_BLOCKS: '112'
      NPRISM_DB_URL: postgres://postgres:postgres@db-baseline:5432/postgres
      NPRISM_DLT_SOURCE_TYPE: oura
      NPRISM_INDEX_INTERVAL: 1s
      RUST_LOG: info,oura=warn,tracing::span=warn
    healthcheck:
      interval: 2s
      retries: 30
      test:
      - CMD
      - curl
      - -f
      - http://localhost:8080/api/_system/health
      timeout: 5s
    image: ${NEOPRISM_BASELINE_IMAGE:-hyperledgeridentus/identus-neoprism:0.14.2}
    restart: always
  candidate:
    command:
    - indexer
    depends_on:
      db-candidate:
        condition: service_healthy
    environment:
      NPRISM_CARDANO_NETWORK: ${NEOPRISM_PARITY_NETWORK:-preprod}
      NPRISM_CARDANO_RELAY_ADDR: ${NEOPRISM_PARITY_RELAY_ADDR:-preprod-node.play.dev.cardano.org:3001}
      NPRISM_CONFIRMATION_BLOCKS: '112'
      NPRISM_DB_URL: postgres://postgres:postgres@db-candidate:5432/postgres
      NPRISM_DLT_SOURCE_TYPE: oura
      NPRISM_INDEX_INTERVAL: 1s
      RUST_LOG: info,oura=warn,tracing::span=warn
    healthcheck:
      interval: 2s
      retries: 30
      test:
      - CMD
      - curl
      - -f
      - http://localhost:8080/api/_system/health
      timeout: 5s
    image: ${NEOPRISM_CANDIDATE_IMAGE:?candidate image is required}
    restart: always
  db-baseline:
    environment:
      POSTGRES_DB: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
    healthcheck:
      interval: 2s
      retries: 30
      test:
      - CMD
      - pg_isready
      - -U
      - postgres
      timeout: 5s
    image: postgres:16
    restart: always
  db-candidate:
    environment:
      POSTGRES_DB: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
    healthcheck:
      interval: 2s
      retries: 30
      test:
      - CMD
      - pg_isready
      - -U
      - postgres
      timeout: 5s
    image: postgres:16
    restart: always
