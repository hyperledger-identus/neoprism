# Code generated by Python script. DO NOT EDIT.
services:
  neoprism-standalone:
    command:
    - dev
    environment:
      NPRISM_CARDANO_NETWORK: custom
      NPRISM_DB_URL: sqlite:///var/lib/neoprism/sqlite/neoprism.db
      NPRISM_INDEX_INTERVAL: '1'
      RUST_LOG: oura=warn,tracing::span=warn,info
    healthcheck:
      interval: 2s
      retries: 30
      test:
      - CMD
      - curl
      - -f
      - http://localhost:8080/api/_system/health
      timeout: 5s
    image: identus-neoprism:latest
    ports:
    - 18080:8080
    restart: always
    volumes:
    - neoprism-sqlite:/var/lib/neoprism/sqlite
volumes:
  neoprism-sqlite: {}
